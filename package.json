{
  "name": "credible-light",
  "version": "1.0.0",
  "private": true,
  "description": "Credible Light – Premium Signage & Interior Solutions Web Application",
  "type": "module",
  "engines": {
    "node": ">=20.0.0 <23.0.0"
  },
  "scripts": {
    "build": "npm --prefix client install --include=dev && npm --prefix client run build",
    "start": "node server/server.js",
    "server": "node server/server.js",
    "client": "npm --prefix client run dev"
  },
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "better-sqlite3": "^11.8.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.2",
    "multer": "^1.4.5-lts.1"
  }
}
